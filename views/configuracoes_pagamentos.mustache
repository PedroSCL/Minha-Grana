<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Configurações de Pagamento - Minha Grana</title>
    <link rel="stylesheet" href="/dashboard.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <div class="dashboard-wrapper">
        <aside class="sidebar">
            <div class="sidebar-header">
                <a href="/" class="app-logo">Minha Grana</a>
            </div>
            <nav class="sidebar-nav">
                <ul>
                    <li><a href="/dashboard"><i class="fas fa-th-large"></i> Painel de Controle</a></li>
                    <li><a href="/transacoes"><i class="fas fa-exchange-alt"></i> Transações</a></li>
                    <li><a href="/perfil"><i class="fas fa-user"></i> Perfil</a></li>
                    <li><a href="/configuracoes-pagamentos" class="active"><i class="fas fa-cog"></i> Configurações</a></li>
                    <li><a href="/logout" class="logout-link"><i class="fas fa-sign-out-alt"></i> Sair</a></li>
                </ul>
            </nav>
        </aside>

        <div class="main-content">
            <header class="dashboard-header">
                <h2>Configurações de Pagamento</h2>
                <div class="header-right">
                    <div class="user-profile">
                        <span>{{nomeUsuario}}</span>
                        <div class="user-profile-initials">
                            <span>{{iniciaisUsuario}}</span>
                        </div>
                    </div>
                </div>
            </header>

            <div class="form-card">
                <a href="/dashboard" class="btn-voltar">← Voltar para o Dashboard</a>

                <form action="/configuracoes-pagamentos" method="POST">
                    <p class="section-description">Defina o valor máximo que você gostaria de gastar para cada categoria de pagamento.</p>

                    <div class="form-group">
                        <label for="Conta">Meta para Contas:</label>
                        <input type="number" id="Conta" name="Conta" step="0.01" value="{{paymentTargets.Conta.target_amount}}" required>
                    </div>

                    <div class="form-group">
                        <label for="Assinaturas">Meta para Assinaturas:</label>
                        <input type="number" id="Assinaturas" name="Assinaturas" step="0.01" value="{{paymentTargets.Assinaturas.target_amount}}" required>
                    </div>

                    <div class="form-group">
                        <label for="Aluguel">Meta para Aluguel:</label>
                        <input type="number" id="Aluguel" name="Aluguel" step="0.01" value="{{paymentTargets.Aluguel.target_amount}}" required>
                    </div>

                    <div class="form-group">
                        <label for="Comida">Meta para Comida:</label>
                        <input type="number" id="Comida" name="Comida" step="0.01" value="{{paymentTargets.Comida.target_amount}}" required>
                    </div>
                    
                    {{#successMessage}}
                        <p style="color: var(--green-primary); font-weight: 600; margin-top: 1em;">{{successMessage}}</p>
                    {{/successMessage}}
                    {{#errorMessage}}
                        <p style="color: var(--red-expense); font-weight: 600; margin-top: 1em;">{{errorMessage}}</p>
                    {{/errorMessage}}

                    <button type="submit">Salvar Metas de Pagamento</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
